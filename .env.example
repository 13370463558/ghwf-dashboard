# 容器部署环境变量（在你容器的 Environment 里配置，或在 /home/container/.env）
# 注意：容器版 cookie 不带 Secure，走的是 HTTP 域名也能登录

# GitHub classic token：最小权限（私有仓库用 repo，只看公开用 public_repo）
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# 登录密码（单账户）
APP_PASSWORD=change-me

# cookie 签名密钥（openssl rand -hex 32 生成）
AUTH_SECRET=change-me-too

# 端口（容器一般通过 PORT 环境变量自动给，有就保留，没有默认 8080）
# PORT=8080