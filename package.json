{
  "name": "ghwf-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GitHub Actions workflow monitor dashboard (Cloudflare Pages + self-hosted container)",
  "scripts": {
    "start": "node server.js",
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "deploy:cf": "npx wrangler pages deploy dist --project-name ghwf-dashboard"
  },
  "dependencies": {
    "cron-parser": "5.10.0",
    "js-yaml": "5.4.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^8.3.0"
  }
}